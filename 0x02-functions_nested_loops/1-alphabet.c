#include "main.h"

/**
 * print_alphabet -this is the function for this code
 * Return: Always 0.
 */

void print_alphabet(void)
{
	char d;

	for (d = 'a' ; d <= 'z'; d++)
	{	_putchar(d);
	}
	_putchar('\n');
}
